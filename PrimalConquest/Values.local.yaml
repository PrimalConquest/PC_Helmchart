global:
  ingress:
    host: 172.21.44.89
    tls:
      enabled: false
      certManager:
        enabled: false

agones:
  enabled: true
 
agonesConfig:
  fleet:
    replicas: 1                 # one warm server is enough locally
  autoscaler:
    bufferSize: 1
    minReplicas: 1
    maxReplicas: 5
     # fine for local, not for prod
 
database:
  authsecret:
    jwtSecret: "VtectquSxoM6e3g4xfZH3oGzLP9u45pX1gAuMXys+qs="          # FILL IN once — min 32 chars: openssl rand -base64 32
    internalApiKey: "1a17c986bee282a8bd95af4471a8e184931da6fb"     # FILL IN once — any non-empty string: openssl rand -hex 20

matchmaking:
  queueTimeoutSeconds: 30        # shorter timeout during development